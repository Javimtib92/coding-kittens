---
import { LanguageSwitchButtonDropdown } from '~/components/react/LanguageSwitchButtonDropdown';

const locale = Astro.currentLocale || 'en';
---

<LanguageSwitchButtonDropdown
  client:only='react'
  locale={locale}
  transition:persist
/>
